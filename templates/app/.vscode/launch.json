{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch CMake Target (Windows, MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "preLaunchTask": "CMake: build"
        },
        {
            "name": "Launch CMake Target (Unix / gdb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build",
            "environment": [],
            "MIMode": "gdb"
        }
    ]
}